// Select the circular text container
const scrollCircle = document.getElementById('scrollCircle');

// Add spacing to repeat the text around the circle
const text = "Scroll Down ";
scrollCircle.innerHTML = text.repeat(5);